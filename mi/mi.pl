% Only load once in Starlog, load before List Prolog.
:- use_module('../../Starlog/starlog').

% Maths Induction

% Maths Induction takes a Prolog algorithm and replaces recursive steps with +, *, etc. with a single formula replacing the recursion. It can also combine these formulas.
